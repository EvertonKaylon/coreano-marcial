# contracts — Project PRD (001-add-prd)

This feature is documentation-only and does not introduce any API or contract changes.

If follow-up features are proposed that add endpoints or APIs, create OpenAPI contract files in this directory (e.g., `openapi.yaml`) and include tests under `tests/contract/`.
